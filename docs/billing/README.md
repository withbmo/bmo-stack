# Billing

This folder documents the **billing + entitlements** system in the API.

## Docs

- `overview.md`: end-to-end flow (Stripe → webhooks → DB → entitlements)
- `data-model.md`: Prisma models and how they relate
- `api.md`: API endpoints and expected usage
- `webhooks.md`: webhook verification, deduping, and handled events
- `entitlements.md`: plan resolution and usage counters

